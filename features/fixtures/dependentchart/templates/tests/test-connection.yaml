apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dependentchart.fullname" . }}-test-connection"
  labels:
    {{- include "dependentchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dependentchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

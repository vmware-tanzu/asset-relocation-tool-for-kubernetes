apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dependent-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dependent-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dependent-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

- "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
- "{{ .samplechart.image.repository }}:{{ .samplechart.image.tag }}"
- "{{ .samplechart.wellDefinedImage.registry }}/{{ .samplechart.wellDefinedImage.repository }}:{{ .samplechart.wellDefinedImage.tag }}"
- "{{ .samplechart.singleTemplateImage.image }}"

apiVersion: v1
kind: Service
metadata:
  name: {{ include "remotechart.fullname" . }}
  labels:
    {{- include "remotechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remotechart.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remotechart.fullname" . }}-test-connection"
  labels:
    {{- include "remotechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "remotechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
